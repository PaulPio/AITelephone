{
  "buildCommand": "cd ../.. && npm install && npm run build -w @drift/shared && npm run build -w @drift/web",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
